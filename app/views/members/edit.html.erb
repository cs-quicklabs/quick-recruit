<section class="bg-white dark:bg-gray-900">
  <div class="py-4 px-4 mx-auto max-w-2xl lg:py-4">
    <%= render "shared/form_messages", resource: @form, message: local_assigns[:message] %>
    <%= render "shared/form_header", title: "Edit Details", subtitle: "Please update the details of team member." %>
    <%= tailwind_form_with(model: @user, id: dom_id(@user), url: member_path(@user)) do |form| %>
      <div class="mt-4 grid gap-4 sm:grid-cols-2 sm:gap-6">
        <div><%= form.text_field :first_name, "First Name", required: true %></div>

        <div><%= form.text_field :last_name, "Last Name", required: true %></div>

        <div><%= form.text_field :phone, "Phone", required: true %></div>

        <div><%= form.text_field :location, "Location", required: true %></div>
      </div>

      <button type="submit" class="btn-primary mt-4">
        Update Member
      </button>

      <%= link_to "Cancel", :back, class: "btn-secondary" %>
    <% end %>
  </div>
</section>
