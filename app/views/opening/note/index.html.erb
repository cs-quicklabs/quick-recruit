<!--
  This example requires updating your template:

  ```
  <html class="h-full bg-white">
  <body class="h-full">
  ```
-->
<div class="flex min-h-full flex-col">
  <!-- 3 column wrapper -->
  <div class="mx-auto w-full grow lg:flex xl:px-2 -ml-4">
    <!-- Left sidebar & main wrapper -->
    <div class="w-80 xl:flex min-h-screen">
      <%= render partial: "opening/profile", locals: { opening: @opening } %>
    </div>
    <div class="flex-1">
      <!-- Main area -->
      <%= render partial: "opening/nav", locals: { opening: @opening } %>
      <div class="p-6 text-sm">
        <%= @opening.note %>
      </div>
    </div>
    <div class="shrink-0 border-t border-gray-200 px-4 py-6 sm:px-6 lg:w-80 lg:border-l lg:border-t-0 lg:pr-8 xl:pr-6">
      <!-- Right column area -->
      <div>
        <h5 class="mr-3 font-semibold dark:text-white">Edit Internal Note </h5>
        <p class="text-gray-500 dark:text-gray-400 text-sm mb-4">Intenal note is a note which is visible to recruitment team members internally. </p>
        <div class="flex space-x-2">
          <a type="button" href="<%= edit_opening_note_path(@opening, @opening) %>"
                class="btn-primary">
            Edit Note
          </a>

        </div>
      </div>
    </div>
  </div>
</div>
